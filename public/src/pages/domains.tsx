import { h } from 'preact';
import Wrapper from '../components/wrapper';
// import useSWR from 'swr';

function Domains() {
  // const { data, error } = useSWR('/api/domains')

  return (
    <div className="container">
      <Wrapper>
        divsdsjdlksd
      </Wrapper>
    </div>
  );
}

export default Domains;
